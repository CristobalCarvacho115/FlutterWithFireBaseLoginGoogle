import 'package:flutter/material.dart';

class RecetasPropias extends StatelessWidget {
  const RecetasPropias({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
